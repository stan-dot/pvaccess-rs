pub mod header;
pub mod messages;
pub mod channel;

pub mod codec;
pub mod frame;
pub mod pv_fielddesc;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
